{
  "name": "express-log-to-json",
  "description": "Filter out noise from express req before logging.",
  "version": "1.0.1",
  "author": "Tim Allen <tim@noblesamurai.com>",
  "license": "BSD",
  "main": "index.js",
  "scripts": {
    "coverage": "nyc -a -c -r html -r text npm test",
    "pretest": "semistandard --env mocha",
    "test": "mocha --recursive test",
    "watch": "mocha --recursive --watch test",
    "docs": "jsdoc2md index.js"
  },
  "homepage": "https://github.com/noblesamurai/express-log-to-json",
  "repository": {
    "type": "git",
    "url": "git://github.com/noblesamurai/express-log-to-json.git"
  },
  "bugs": {
    "url": "https://github.com/noblesamurai/express-log-to-json/issues"
  },
  "engines": {
    "node": ">= 6.x",
    "npm": "3.x"
  },
  "peerDependencies": {
    "express": "*"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "dirty-chai": "^1.2.2",
    "express": "^4.15.3",
    "jsdoc-to-markdown": "~3.0.0",
    "mocha": "~3.3.0",
    "nyc": "^10.1.2",
    "semistandard": "*",
    "supertest": "^3.0.0"
  },
  "keywords": [
    "express",
    "log",
    "filter",
    "json"
  ],
  "nyc": {
    "exclude": [
      "coverage",
      "test"
    ]
  }
}
